class ChatList{
  final String name;
  final String lastMessage;
  final bool isgroup;
  final bool isOnline;
  final List<String> avatar;
  final bool isRead;

  ChatList({this.name, this.lastMessage, this.isgroup, this.isOnline, this.avatar,this.isRead, });
}